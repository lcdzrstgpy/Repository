# 接口契约 · 一阶段

> 本文档是后端、前端、数据库三方的**唯一约定**。所有实现必须严格按此文档，不得自行更改字段名、状态值、路径。
> 如需变更，先改本文档，再改代码。

---

## 一、技术栈约定

| 层 | 选型 | 版本 |
|---|---|---|
| 后端 | Python + FastAPI + SQLAlchemy 2.0 + Pydantic v2 | Python 3.12 |
| 数据库 | MySQL 8 | 8.0 |
| 认证 | JWT（HS256） | python-jose |
| 前端 | Vue 3 + Vite + Element Plus + Pinia + Axios | Vue 3.4+ |
| 后端端口 | 8000 | |
| 前端端口 | 5173 | |

---

## 二、统一响应格式

**所有接口**（含错误）统一返回：

```json
{
  "code": 0,
  "msg": "ok",
  "data": {}
}
```

| code | 含义 |
|---|---|
| 0 | 成功 |
| 400 | 参数错误 |
| 401 | 未登录 / token 失效 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |
| 1001 | 业务错误（如状态流转不合法），msg 为具体原因 |

分页响应 `data` 结构：

```json
{
  "list": [],
  "total": 100,
  "page": 1,
  "page_size": 20
}
```

分页请求统一用 query 参数：`?page=1&page_size=20`。

---

## 三、枚举定义

### 3.1 用户角色 `sys_user.role`

| 值 | 含义 |
|---|---|
| `admin` | 管理员，全部权限 |
| `operator` | 运营，下单 / 看自己的订单 / 确认完成 |
| `warehouse` | 仓储，接单 / 备货 / 发货 / 看库存 |
| `approver` | 审批（一阶段仅建数据，不实现界面） |

### 3.2 订单状态 `sales_order.status`

| 值 | 状态 | 谁操作 | 说明 |
|---|---|---|---|
| 10 | 待接单 | 运营提交后自动进入 | |
| 20 | 已接单 | 仓储 | 写 `claimed_by` / `claimed_at` |
| 30 | 备货中 | 仓储 | 写 `prepare_at` |
| 40 | 已发货 | 仓储 | 写 `shipped_at` / `express_no`，**回传点** |
| 50 | 已完成 | 运营 | 写 `finished_at` |
| 90 | 已取消 | 运营 / 仓储 | 写 `cancel_reason` |

### 3.3 审批状态 `audit_status`

| 值 | 含义 |
|---|---|
| 0 | 未审批 |
| 1 | 已审批 |

### 3.4 往来单位类型 `partner.type`

| 值 | 含义 |
|---|---|
| 1 | 客户 |
| 2 | 供应商 |
| 3 | 两者都是 |

### 3.5 通用状态 `status`（基础数据表）

| 值 | 含义 |
|---|---|
| 1 | 启用 |
| 0 | 停用 |

---

## 四、数据模型

> 所有表统一含 `created_at`（datetime，默认当前时间）、`updated_at`（datetime，更新时刷新）。
> 主键统一 `id` bigint 自增。
> 金额统一 `decimal(14,2)`，数量统一 `decimal(14,2)`。

### 4.1 `sys_user` 用户

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| username | varchar(50) UNIQUE | 登录名 |
| password_hash | varchar(255) | bcrypt 哈希 |
| real_name | varchar(50) | 姓名 |
| role | varchar(20) | 见 3.1 |
| status | tinyint | 1 启用 / 0 停用 |

### 4.2 `warehouse` 仓库

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| code | varchar(32) UNIQUE | 仓库编码 |
| name | varchar(100) | 仓库名称 |
| address | varchar(255) | 地址 |
| status | tinyint | 1 启用 / 0 停用 |

### 4.3 `product` 商品

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| code | varchar(64) UNIQUE | 商品编码 |
| name | varchar(200) | 商品名称 |
| category | varchar(50) | 分类 |
| unit | varchar(20) | 单位 |
| status | tinyint | 1 启用 / 0 停用 |

### 4.4 `product_sku` SKU

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| product_id | bigint | 关联 product |
| sku_code | varchar(64) UNIQUE | SKU 编码 |
| spec | varchar(200) | 规格 |
| price | decimal(14,2) | 售价 |
| status | tinyint | 1 启用 / 0 停用 |

### 4.5 `partner` 往来单位

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| name | varchar(200) | 名称 |
| type | tinyint | 1 客户 / 2 供应商 / 3 两者 |
| contact | varchar(50) | 联系人 |
| phone | varchar(30) | 电话 |
| address | varchar(255) | 地址 |
| status | tinyint | 1 启用 / 0 停用 |

### 4.6 `sales_order` 销售订单

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| no | varchar(32) UNIQUE | 单号，格式 `SO` + yyyyMMdd + 4位流水，如 `SO202609160001` |
| customer_id | bigint | 关联 partner（type 含 1） |
| status | tinyint | 见 3.2，默认 10 |
| audit_status | tinyint | 见 3.3，默认 0 |
| warehouse_id | bigint NULL | 指派仓库，接单时写入 |
| claimed_by | bigint NULL | 接单人 user_id |
| claimed_at | datetime NULL | 接单时间 |
| prepare_at | datetime NULL | 开始备货时间 |
| shipped_at | datetime NULL | 发货时间 |
| finished_at | datetime NULL | 完成时间 |
| express_no | varchar(64) NULL | 物流单号 |
| cancel_reason | varchar(255) NULL | 取消原因 |
| remark | varchar(500) NULL | 备注 |
| total_count | decimal(14,2) | 总数量（由明细汇总） |
| total_price | decimal(14,2) | 总金额（由明细汇总） |
| created_by | bigint | 下单人 user_id |

### 4.7 `sales_order_item` 订单明细

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| order_id | bigint | 关联 sales_order |
| sku_id | bigint | 关联 product_sku |
| count | decimal(14,2) | 下单数量 |
| out_count | decimal(14,2) | 已出库数量，默认 0 |
| price | decimal(14,2) | 单价 |
| total_price | decimal(14,2) | 小计 = count × price |

### 4.8 `inventory` 库存余额（一阶段建表，接口只读）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| sku_id | bigint | |
| warehouse_id | bigint | |
| quantity | decimal(14,2) | 当前库存，默认 0 |
| reserved_quantity | decimal(14,2) | 预留库存，默认 0 |

**UNIQUE(sku_id, warehouse_id)**

### 4.9 `inventory_history` 库存流水（一阶段建表，暂不写入）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| sku_id | bigint | |
| warehouse_id | bigint | |
| quantity | decimal(14,2) | 变动量，正入负出 |
| before_quantity | decimal(14,2) | 变动前 |
| after_quantity | decimal(14,2) | 变动后 |
| order_no | varchar(32) | 关联单号 |
| order_type | varchar(20) | 单据类型 |
| created_by | bigint | 操作人 |

---

## 五、接口清单

> 除 `/api/auth/login` 外，**所有接口**需请求头 `Authorization: Bearer <token>`。

### 5.1 认证

#### POST `/api/auth/login`

请求：
```json
{ "username": "admin", "password": "admin123" }
```

响应 data：
```json
{
  "access_token": "eyJ...",
  "token_type": "bearer",
  "user": { "id": 1, "username": "admin", "real_name": "管理员", "role": "admin" }
}
```

#### GET `/api/auth/me`

响应 data：当前用户对象（同上 `user` 结构）。

#### POST `/api/auth/logout`

响应 data：`null`

---

### 5.2 基础数据 CRUD

以下五组接口结构完全一致（以 `warehouse` 为例），全部仅 `admin` 可写，其他角色只读。

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/warehouses` | 列表，支持 `?page=&page_size=&keyword=` |
| GET | `/api/warehouses/{id}` | 详情 |
| POST | `/api/warehouses` | 新增 |
| PUT | `/api/warehouses/{id}` | 修改 |
| DELETE | `/api/warehouses/{id}` | 删除（软删，置 status=0） |

同样的五组路径替换为：
- `/api/products`（商品）
- `/api/skus`（SKU）
- `/api/partners`（往来单位）
- `/api/users`（用户，仅 admin 可访问全部方法）

**额外接口：**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/partners/options?type=1` | 下拉选项，返回 `[{id, name}]`，type 可选 |
| GET | `/api/skus/options` | SKU 下拉选项，返回 `[{id, sku_code, name, spec, price}]`，name 取所属 product.name |
| GET | `/api/warehouses/options` | 仓库下拉选项，返回 `[{id, name}]` |

---

### 5.3 运营侧 · 订单

#### POST `/api/sales-orders` 创建订单

请求：
```json
{
  "customer_id": 1,
  "remark": "加急",
  "items": [
    { "sku_id": 1, "count": 10, "price": 25.00 },
    { "sku_id": 2, "count": 5,  "price": 18.50 }
  ]
}
```

响应 data：订单详情对象（见下）。

**规则**：`created_by` 取当前登录用户；`status` 固定初始化 10；`total_count` / `total_price` 由后端按明细汇总计算，前端传的不信任。

#### GET `/api/sales-orders` 订单列表

Query：`?page=1&page_size=20&status=&keyword=`

**可见性规则（重要）**：
- `operator`：**只返回 `created_by = 当前用户` 的订单**
- `admin`：返回全部
- `warehouse`：调用此接口返回全部（但仓储端主要用 5.4 的待接单接口）

响应 data 的 `list` 元素结构：
```json
{
  "id": 1,
  "no": "SO202609160001",
  "customer_name": "某某贸易",
  "status": 20,
  "status_text": "已接单",
  "total_count": 15.00,
  "total_price": 342.50,
  "express_no": null,
  "created_by_name": "张三",
  "claimed_by_name": "李四",
  "created_at": "2026-09-16 10:00:00",
  "shipped_at": null
}
```

#### GET `/api/sales-orders/{id}` 订单详情

响应 data 在列表元素基础上增加：
```json
{
  "remark": "加急",
  "warehouse_id": 1,
  "warehouse_name": "主仓",
  "claimed_at": "2026-09-16 10:05:00",
  "prepare_at": null,
  "finished_at": null,
  "cancel_reason": null,
  "items": [
    {
      "id": 1,
      "sku_id": 1,
      "sku_code": "SKU001",
      "product_name": "商品A",
      "spec": "红色/大号",
      "count": 10.00,
      "out_count": 0.00,
      "price": 25.00,
      "total_price": 250.00
    }
  ]
}
```

#### POST `/api/sales-orders/{id}/cancel` 取消订单

请求：`{ "cancel_reason": "客户改需求" }`

**规则**：仅 `status` 在 `10 / 20 / 30` 时可取消；`operator` 只能取消自己的订单。成功后 `status = 90`。

#### POST `/api/sales-orders/{id}/confirm` 确认完成

请求：`{}`

**规则**：仅 `status = 40` 时可确认；仅 `operator`（且是自己的订单）或 `admin`。成功后 `status = 50`，写 `finished_at`。

---

### 5.4 仓储侧 · 订单处理

#### GET `/api/warehouse/pending-orders` 待接单列表

Query：`?page=1&page_size=20`

**规则**：返回 `status = 10` 的全部订单（不分下单人）。仅 `warehouse` / `admin` 可访问。

响应元素结构同 5.3 列表。

#### POST `/api/warehouse/orders/{id}/claim` 接单

请求：`{ "warehouse_id": 1 }`

**规则**：仅 `status = 10` 可接单；成功后 `status = 20`，写 `claimed_by`（当前用户）、`claimed_at`、`warehouse_id`。仅 `warehouse` / `admin`。

#### POST `/api/warehouse/orders/{id}/prepare` 开始备货

请求：`{}`

**规则**：仅 `status = 20`；成功后 `status = 30`，写 `prepare_at`。仅 `warehouse` / `admin`。

#### POST `/api/warehouse/orders/{id}/ship` 发货

请求：
```json
{ "express_no": "SF1234567890" }
```

**规则**：仅 `status = 30`；成功后 `status = 40`，写 `shipped_at`、`express_no`。仅 `warehouse` / `admin`。

**这是状态回传点**——运营端轮询列表即可看到状态变为"已发货"。

---

### 5.5 库存查询

#### GET `/api/inventory` 库存列表

Query：`?page=1&page_size=20&warehouse_id=&keyword=`

响应 data 的 `list` 元素：
```json
{
  "id": 1,
  "sku_id": 1,
  "sku_code": "SKU001",
  "product_name": "商品A",
  "spec": "红色/大号",
  "warehouse_id": 1,
  "warehouse_name": "主仓",
  "quantity": 100.00,
  "reserved_quantity": 0.00,
  "available_quantity": 100.00
}
```

> `available_quantity = quantity - reserved_quantity`，后端计算。

#### GET `/api/inventory/history` 库存流水

Query：`?page=1&page_size=20&sku_id=&warehouse_id=`

---

## 六、状态流转合法性校验

后端**必须**在每个流转接口里校验当前状态，不合法时返回 `code = 1001`，msg 明确说明，例如：

```
"订单当前状态为「已接单」，无法执行接单操作"
```

合法流转表：

| 操作 | 允许的当前状态 | 目标状态 | 允许角色 |
|---|---|---|---|
| 接单 | 10 | 20 | warehouse / admin |
| 备货 | 20 | 30 | warehouse / admin |
| 发货 | 30 | 40 | warehouse / admin |
| 确认完成 | 40 | 50 | operator（本人）/ admin |
| 取消 | 10 / 20 / 30 | 90 | operator（本人）/ warehouse / admin |

---

## 七、初始化数据

首次启动需写入：

**用户**（密码统一 `admin123`，bcrypt 存储）
| username | real_name | role |
|---|---|---|
| admin | 管理员 | admin |
| operator1 | 运营小王 | operator |
| warehouse1 | 仓管老李 | warehouse |

**仓库**：`WH001 / 主仓`、`WH002 / 备用仓`

**商品 + SKU**：3 个商品，各 1 个 SKU，编码 `SKU001` ~ `SKU003`

**往来单位**：2 个客户（type=1）、2 个供应商（type=2）

**库存**：为 3 个 SKU 在主仓各写入 `quantity = 100`

**示例订单**：1 条 `status = 10` 的待接单订单，供仓储端测试。

---

## 八、CORS 与联调

后端必须开启 CORS，允许 `http://localhost:5173`，允许 `Authorization` 头。

前端 Vite 配置 proxy：`/api` → `http://localhost:8000`。

---

# 二、三阶段契约（库存 / 出库 / 采购）

> 以下章节是第一阶段契约的**增量补充**，第一至八节仍然有效。

## 九、二阶段：库存与出库单

### 9.1 设计说明

一阶段的「发货」接口在二阶段**增强**为真实库存操作。`POST /api/warehouse/orders/{id}/ship` 被调用后，后端依次执行：

1. 校验库存是否充足（不足返回 `code=1001`，提示缺哪个 SKU、缺多少）
2. 批量扣减库存 + 写库存流水（同一事务）
3. 自动生成一张**已完成**的出库单（`sales_out`），作为发货凭证
4. 订单转「已发货」，回写明细 `out_count`

**前端不需要改动发货接口的调用方式**，只是后端内部行为变重了。

### 9.2 新增表

**`sales_out` 出库单**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| no | varchar(32) UNIQUE | 单号 `OUT` + yyyyMMdd + 4 位流水 |
| order_id | bigint | 关联销售订单 |
| order_no | varchar(32) | 冗余订单号 |
| warehouse_id | bigint | 出库仓库 |
| status | tinyint | 10 草稿 / 20 已完成 / 90 已作废 |
| total_count | decimal(14,2) | 总数量 |
| total_price | decimal(14,2) | 总金额 |
| express_no | varchar(64) | 物流单号 |
| remark | varchar(500) | |
| created_by | bigint | |
| created_at / updated_at | datetime | |

**`sales_out_item` 出库明细**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| out_id | bigint | 关联 sales_out |
| order_item_id | bigint | 关联 sales_order_item |
| sku_id | bigint | |
| count | decimal(14,2) | 出库数量 |
| price | decimal(14,2) | 单价 |
| total_price | decimal(14,2) | 小计 |
| created_at / updated_at | datetime | |

### 9.3 库存变更服务（共享，采购模块也要调用）

所有库存变动**必须**走这个统一入口，禁止在业务代码里直接 UPDATE 库存表。

```python
def change_inventory(
    db,                     # Session
    changes: list[dict],    # [{"sku_id": 1, "warehouse_id": 1, "quantity": 10}]  正数入库，负数出库
    order_no: str,          # 关联单号
    order_type: str,        # SALES_OUT / PURCHASE_IN / ADJUST
    operator_id: int,       # 操作人
) -> None
```

实现要求（严格按此，这是抄 yudao WMS 的核心）：

1. **去重合并**：同一个 `(sku_id, warehouse_id)` 出现多行时，先在内存里合并成一个变更量，减少数据库交互
2. **懒创建**：库存行不存在时，先尝试插入 `quantity=0` 的记录，捕获 `IntegrityError` 后重新查询。**不要"先查后插"**（有竞态）
3. **批量悲观锁**：收集所有待锁定的库存行 id，一次性 `SELECT ... FOR UPDATE` 锁定。**禁止在循环里逐行加锁**（会死锁）
4. **校验**：出库时校验 `quantity - reserved_quantity >= 出库量`，不足抛业务异常，错误信息必须带 SKU 编码、仓库名、当前可用量、需求量
5. **更新 + 写流水**：更新库存，同时为每笔变动写 `inventory_history`（记 `before_quantity` / `after_quantity`）

### 9.4 新增接口

| 方法 | 路径 | 角色 | 说明 |
|---|---|---|---|
| GET | `/api/sales-outs` | warehouse / admin | 出库单列表，支持 `?page=&page_size=&keyword=&warehouse_id=` |
| GET | `/api/sales-outs/{id}` | warehouse / admin | 出库单详情（含明细） |
| POST | `/api/sales-outs/{id}/cancel` | warehouse / admin | 作废出库单，**回滚库存**（反向 change_inventory），关联订单状态回退到「备货中」 |

**库存流水响应字段**（补齐一阶段缺口）—— `GET /api/inventory/history` 的 `list` 元素：

```json
{
  "id": 1,
  "sku_id": 1,
  "sku_code": "SKU001",
  "product_name": "商品A",
  "warehouse_id": 1,
  "warehouse_name": "主仓",
  "quantity": -10.00,
  "before_quantity": 100.00,
  "after_quantity": 90.00,
  "order_no": "OUT202609160001",
  "order_type": "SALES_OUT",
  "order_type_text": "销售出库",
  "created_by": 1,
  "created_by_name": "仓管老李",
  "created_at": "2026-09-16 10:00:00"
}
```

`order_type` 取值与中文名对照：

| 值 | 中文 |
|---|---|
| SALES_OUT | 销售出库 |
| PURCHASE_IN | 采购入库 |
| ADJUST | 库存调整 |

**手动调整库存（盘点用）**

| 方法 | 路径 | 角色 | 说明 |
|---|---|---|---|
| POST | `/api/inventory/adjust` | admin | 请求 `{sku_id, warehouse_id, quantity, remark}`。`quantity` 是**目标值**，后端算差异后走 `change_inventory` |

---

## 十、三阶段：采购

### 10.1 设计说明

备货时若库存不足，仓储端可发起采购：

```
创建采购单 → 审批 → 收货入库（库存增加）→ 订单继续备货
```

**采购单本身不影响库存**，只有「收货入库」才增加库存。这是抄 yudao ERP 的关键设计——订单类操作和库存类操作严格分离。

### 10.2 新增表

**`purchase_order` 采购单**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| no | varchar(32) UNIQUE | 单号 `PO` + yyyyMMdd + 4 位流水 |
| supplier_id | bigint | 供应商（partner.type 含 2） |
| sales_order_id | bigint NULL | 关联销售订单（因缺货采购时写入） |
| status | tinyint | 10 待审批 / 20 已审批 / 30 已入库 / 90 已取消 |
| total_count | decimal(14,2) | |
| total_price | decimal(14,2) | |
| expect_date | date NULL | 期望到货日期 |
| remark | varchar(500) | |
| created_by | bigint | 创建人 |
| approved_by | bigint NULL | 审批人 |
| approved_at | datetime NULL | 审批时间 |
| created_at / updated_at | datetime | |

**`purchase_order_item` 采购明细**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| order_id | bigint | 关联 purchase_order |
| sku_id | bigint | |
| count | decimal(14,2) | 采购数量 |
| in_count | decimal(14,2) | 已入库数量，默认 0 |
| price | decimal(14,2) | |
| total_price | decimal(14,2) | |
| created_at / updated_at | datetime | |

**`purchase_in` 采购入库单**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| no | varchar(32) UNIQUE | 单号 `IN` + yyyyMMdd + 4 位流水 |
| order_id | bigint | 关联采购单 |
| order_no | varchar(32) | |
| warehouse_id | bigint | 入库仓库 |
| status | tinyint | 20 已完成（简化为创建即入库） |
| total_count | decimal(14,2) | |
| total_price | decimal(14,2) | |
| remark | varchar(500) | |
| created_by | bigint | |
| created_at / updated_at | datetime | |

**`purchase_in_item` 入库明细**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK | |
| in_id | bigint | 关联 purchase_in |
| order_item_id | bigint | 关联 purchase_order_item |
| sku_id | bigint | |
| count | decimal(14,2) | |
| price | decimal(14,2) | |
| total_price | decimal(14,2) | |
| created_at / updated_at | datetime | |

### 10.3 新增接口

| 方法 | 路径 | 角色 | 说明 |
|---|---|---|---|
| POST | `/api/purchase-orders` | warehouse / admin | 创建采购单 |
| GET | `/api/purchase-orders` | warehouse / approver / admin | 列表，支持 `?page=&page_size=&status=&keyword=` |
| GET | `/api/purchase-orders/{id}` | 同上 | 详情（含明细） |
| POST | `/api/purchase-orders/{id}/approve` | approver / admin | 审批通过，`status` 10 → 20 |
| POST | `/api/purchase-orders/{id}/cancel` | warehouse / admin | 取消，`status` 10/20 → 90 |
| POST | `/api/purchase-orders/{id}/receive` | warehouse / admin | 收货入库 |
| GET | `/api/purchase-ins` | warehouse / admin | 入库单列表 |
| GET | `/api/purchase-ins/{id}` | warehouse / admin | 入库单详情 |

**创建采购单请求**：

```json
{
  "supplier_id": 1,
  "sales_order_id": 5,
  "expect_date": "2026-09-20",
  "remark": "补货",
  "items": [
    { "sku_id": 1, "count": 50, "price": 12.00 }
  ]
}
```

**收货入库请求**：

```json
{
  "warehouse_id": 1,
  "remark": "全部到货",
  "items": [
    { "order_item_id": 1, "count": 50 }
  ]
}
```

收货入库的处理逻辑（按顺序）：

1. 校验采购单 `status = 20`（已审批），否则 `code=1001`
2. 校验本次入库量不超过 `count - in_count`
3. 走 `change_inventory` 增加库存（正数）
4. 生成 `purchase_in` + `purchase_in_item`
5. 回写 `purchase_order_item.in_count`
6. 若所有明细都 `in_count == count`，采购单 `status` → 30 已入库

**列表元素结构**（采购单）：

```json
{
  "id": 1,
  "no": "PO202609160001",
  "supplier_name": "某供应商",
  "sales_order_no": "SO202609160001",
  "status": 20,
  "status_text": "已审批",
  "total_count": 50.00,
  "total_price": 600.00,
  "expect_date": "2026-09-20",
  "created_by_name": "仓管老李",
  "approved_by_name": "管理员",
  "created_at": "2026-09-16 10:00:00"
}
```

采购单状态中文名：`10 待审批 / 20 已审批 / 30 已入库 / 90 已取消`

### 10.4 补齐一阶段缺口：用户请求体

| 方法 | 路径 | 请求体 |
|---|---|---|
| POST | `/api/users` | `{username, password, real_name, role, status}` |
| PUT | `/api/users/{id}` | `{real_name, role, status, password?}` —— `password` 省略或传空串表示不修改密码 |

---

## 十一、角色权限总表

| 接口域 | admin | operator | warehouse | approver |
|---|---|---|---|---|
| 认证 | 全部 | 全部 | 全部 | 全部 |
| 基础数据读写 | ✅ | ❌ | ❌ | ❌ |
| 基础数据只读 | ✅ | ✅ | ✅ | ✅ |
| 订单（创建/取消/确认） | ✅ | ✅（仅自己的） | ❌ | ❌ |
| 订单列表 | 全部 | 仅自己 | 全部 | ❌ |
| 仓储接单/备货/发货 | ✅ | ❌ | ✅ | ❌ |
| 出库单 | ✅ | ❌ | ✅ | ❌ |
| 库存查询 | ✅ | ✅（只读） | ✅ | ❌ |
| 库存调整 | ✅ | ❌ | ❌ | ❌ |
| 采购单创建/取消/收货 | ✅ | ❌ | ✅ | ❌ |
| 采购单审批 | ✅ | ❌ | ❌ | ✅ |
| 采购单列表/详情 | ✅ | ❌ | ✅ | ✅ |

---

# 四阶段契约（库存预留 / 打印 / 导入导出）

## 十二、库存预留机制

### 12.1 要解决的问题

当前实现里，**创建订单时不预扣库存，只在发货时才扣**。这意味着两张待发货的订单可以同时"看到"同一批货，先发货的成功，后发货的才失败——对仓储端来说是很差的体验（货都拣好了才发现不够）。

### 12.2 方案

`inventory.reserved_quantity` 字段（第一阶段就建好了，一直没用）投入使用：

| 时点 | 库存操作 |
|---|---|
| 仓储接单（status 10 → 20） | **预留**：`reserved_quantity += 需求量`，`quantity` 不变 |
| 发货（status 30 → 40） | **转实扣**：`reserved_quantity -= 需求量`，同时 `quantity -= 需求量`，写流水 |
| 订单取消（status → 90，且已接单） | **释放**：`reserved_quantity -= 需求量` |
| 出库单作废 | **回滚实扣**：`quantity += 需求量`（预留早在发货时就释放了，此处不再动 `reserved_quantity`） |

**可用量定义**：`available_quantity = quantity - reserved_quantity`

**接单时校验**：`available_quantity >= 需求量`，不足则返回 `code=1001`，msg 说明哪个 SKU 在哪个仓库可用量不足、缺多少。

### 12.3 服务层新增两个函数

在 `app/services/inventory_service.py` 里新增（与既有 `change_inventory` 同级）：

```python
def reserve_inventory(db, changes, order_no, operator_id) -> None
    """预留库存：只增加 reserved_quantity，不改 quantity，不写库存流水（预留不是库存变动）。"""

def release_inventory(db, changes, order_no, operator_id) -> None
    """释放预留：减少 reserved_quantity。释放量不得使 reserved_quantity 为负。"""
```

实现要求与 `change_inventory` 一致：去重合并、懒创建（savepoint 兜并发）、**批量** `SELECT ... FOR UPDATE` 加锁、校验后更新。**内部不 commit**，由调用方统一提交。

### 12.4 受影响的既有接口（行为增强，请求/响应格式不变）

| 接口 | 变化 |
|---|---|
| `POST /api/warehouse/orders/{id}/claim` | 接单前校验可用量，接单时执行预留；可用量不足返回 `code=1001` |
| `POST /api/warehouse/orders/{id}/ship` | 发货时先释放预留，再执行实扣 + 写流水（同一事务） |
| `POST /api/sales-orders/{id}/cancel` | 若订单处于已接单（20）或备货中（30），取消时释放预留 |
| `POST /api/sales-outs/{id}/cancel` | 作废时回滚实扣的库存（`quantity +=`），不动 `reserved_quantity` |

---

## 十三、单据打印

前端新增两个**独立布局**的打印路由（不套主布局的侧边栏，复用现有登录态）：

| 路由 | 说明 |
|---|---|
| `/print/sales-out/:id` | 出库单打印页 |
| `/print/purchase-order/:id` | 采购单打印页 |

要求：

- A4 尺寸适配，用 `@media print` 隐藏操作按钮和导航
- 内容包含：单据标题、单号、日期、往来单位、明细表格、合计数量与金额、制单人、备注
- 页面上有「打印」按钮，调 `window.print()`
- 出库单列表和采购单列表的「详情」里，各加一个「打印」按钮跳转到对应打印页

打印页**不需要后端新增接口**，复用已有的详情接口。

---

## 十四、Excel 导入导出

### 14.1 导出

**响应不走统一 `{code,msg,data}` 包装**，直接返回 xlsx 文件流：

- `Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
- `Content-Disposition: attachment; filename="xxx.xlsx"`（文件名用中文 + URL 编码）

| 方法 | 路径 | 角色 | 说明 |
|---|---|---|---|
| GET | `/api/export/products` | admin | 商品列表 |
| GET | `/api/export/skus` | admin | SKU 列表 |
| GET | `/api/export/inventory` | warehouse / admin | 库存，支持 `?warehouse_id=` |
| GET | `/api/export/sales-orders` | operator / warehouse / admin | 订单，支持 `?status=&start_date=&end_date=`；**operator 只导出自己创建的** |

各导出的列：

- **商品**：商品编码 / 商品名称 / 分类 / 单位 / 状态
- **SKU**：SKU 编码 / 商品名称 / 规格 / 售价 / 状态
- **库存**：SKU 编码 / 商品名称 / 规格 / 仓库 / 库存量 / 预留量 / 可用量
- **订单**：单号 / 客户 / 状态 / 总数量 / 总金额 / 物流单号 / 下单人 / 接单人 / 下单时间 / 发货时间

### 14.2 导入

| 方法 | 路径 | 角色 | 说明 |
|---|---|---|---|
| POST | `/api/import/products` | admin | `multipart/form-data`，字段名 `file`，上传 xlsx 批量导入商品 |

**模板列**（第一行为表头，从第二行开始读数据）：

| 商品编码 | 商品名称 | 分类 | 单位 | 状态 |
|---|---|---|---|---|
| P001 | 示例商品 | 日用品 | 个 | 1 |

导入规则：

- 商品编码已存在 → 该行跳过并记错误（**不做更新**）
- 商品编码或名称为空 → 该行跳过并记错误
- 状态列接受 `1` / `0`，空值默认 `1`
- 逐行独立处理，**单行失败不影响其他行**
- 返回结构：

```json
{
  "code": 0,
  "msg": "导入完成",
  "data": {
    "total": 10,
    "success": 8,
    "failed": 2,
    "errors": [
      { "row": 3, "message": "商品编码已存在：P003" },
      { "row": 7, "message": "商品名称不能为空" }
    ]
  }
}
```

> `row` 是 Excel 里的**实际行号**（从 1 开始，表头是第 1 行）。

### 14.3 技术选型

后端用 `openpyxl` 处理 xlsx（**不要用 pandas**，太重）。导出用 `openpyxl.Workbook` 写入 `BytesIO` 后返回 `StreamingResponse`；导入用 `openpyxl.load_workbook` 读取上传文件。

需要把 `openpyxl` 加进 `backend/requirements.txt`。

### 14.4 前端

- 商品管理、SKU 管理、库存查询、订单列表页各加一个「导出 Excel」按钮，调对应导出接口，用 `responseType: 'blob'` 下载（注意：**下载接口不能走现有的响应拦截器逻辑**，需要单独处理，因为返回的是二进制流）
- 商品管理页加「导入 Excel」按钮：上传文件 → 调导入接口 → 用弹窗展示导入结果（成功数、失败数、错误明细列表）
- 再提供一个「下载模板」的说明或按钮（可以是前端直接用 SheetJS 生成，或提示用户按列填写）

---

## 十五、四阶段角色权限补充

| 接口域 | admin | operator | warehouse | approver |
|---|---|---|---|---|
| 导出商品 / SKU | ✅ | ❌ | ❌ | ❌ |
| 导出库存 | ✅ | ❌ | ✅ | ❌ |
| 导出订单 | ✅ | ✅（仅自己的） | ✅ | ❌ |
| 导入商品 | ✅ | ❌ | ❌ | ❌ |
| 单据打印 | ✅ | ✅ | ✅ | ✅ |

---

# 五阶段契约（移库单 / 盘点单 / 库存预警）

> 本阶段全部是「能抄就抄」的部分：
> - 移库单抄 yudao WMS 的 `wms_transfer`（移库单）+ 若依WMS 的「移库」
> - 盘点单抄 yudao WMS 的 `wms_stock_take`（盘库单）+ 若依WMS 的「盘库」
> - 库存预警抄 `vvsgxmn/WarehouseManage` 的安全库存预警
>
> 三块都**不新增库存变更路径**，一律复用既有唯一入口
> `app/services/inventory_service.py::change_inventory()`。

## 十六、移库单（库存调拨）

### 16.1 设计说明

- 一张移库单 = **一个出库仓 → 一个入库仓**，一次可移多个 SKU
- 两阶段状态机（抄 yudao WMS 单据风格）：`草稿(10) → 已完成(20)`；仅草稿可作废 `(90)`
- **只有「执行移库」才动库存**。建单本身不动库存（与采购单同样的设计：单据与库存分离）
- 执行移库在同一事务里：出库仓 `-count`（流水 `TRANSFER_OUT`）、入库仓 `+count`（流水 `TRANSFER_IN`），共写 2×N 条流水
- 出库仓库存不足 → 整体失败，`code=1001`，msg 带 SKU 编码 / 仓库名 / 可用量 / 需求量

### 16.2 新增表

**`stock_transfer` 移库单主表**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键，自增 |
| no | varchar(32) | 单号 `TR` + yyyyMMdd + 4 位当日流水，**唯一** |
| from_warehouse_id | bigint | 出库仓 `warehouse.id` |
| to_warehouse_id | bigint | 入库仓 `warehouse.id` |
| status | tinyint | 10 草稿 / 20 已完成 / 90 已作废 |
| total_count | decimal(14,2) | 合计移库数量 |
| remark | varchar(500) | 备注，可空 |
| created_by | bigint | 制单人 `sys_user.id` |
| finished_at | datetime | 执行移库时间，可空 |
| created_at / updated_at | datetime | 统一时间戳 |

索引：`no` 唯一索引；`status`、`from_warehouse_id`、`to_warehouse_id` 普通索引。

**`stock_transfer_item` 移库单明细**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键，自增 |
| transfer_id | bigint | 关联 `stock_transfer.id` |
| sku_id | bigint | 关联 `product_sku.id` |
| count | decimal(14,2) | 移库数量，> 0 |
| created_at / updated_at | datetime | 统一时间戳 |

索引：`transfer_id` 普通索引。

### 16.3 新增接口

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/api/stock-transfers` | warehouse, admin | 创建移库单（草稿） |
| GET | `/api/stock-transfers` | warehouse, admin | 列表，支持 `?page=&page_size=&keyword=&status=` |
| GET | `/api/stock-transfers/{id}` | warehouse, admin | 详情（含明细） |
| POST | `/api/stock-transfers/{id}/finish` | warehouse, admin | 执行移库（草稿 → 已完成） |
| POST | `/api/stock-transfers/{id}/cancel` | warehouse, admin | 作废（仅草稿 → 已作废） |

`keyword` 匹配：单号 / 备注。

**POST `/api/stock-transfers` 请求**

```json
{
  "from_warehouse_id": 1,
  "to_warehouse_id": 2,
  "remark": "补货到备用仓",
  "items": [
    { "sku_id": 1, "count": 10 },
    { "sku_id": 2, "count": 5 }
  ]
}
```

校验（任一不满足 → `code=1001`）：
1. `from_warehouse_id != to_warehouse_id`，否则「不能移库到同一个仓库」
2. 两个仓库都存在且 `status = 1`，否则「仓库不存在」/「仓库已停用」
3. 明细非空；同一 `sku_id` 不允许重复行（Pydantic 层校验，`code=400`）
4. 每个 SKU 存在且 `status = 1`

`total_count` 由后端按明细汇总，**不接受前端传值**。

**响应结构（列表元素 / 详情）**

```json
{
  "id": 1,
  "no": "TR202609160001",
  "from_warehouse_id": 1,
  "from_warehouse_name": "主仓",
  "to_warehouse_id": 2,
  "to_warehouse_name": "备用仓",
  "status": 10,
  "status_text": "草稿",
  "total_count": 15.00,
  "remark": "补货到备用仓",
  "created_by": 3,
  "created_by_name": "仓管老李",
  "finished_at": null,
  "created_at": "2026-09-16 20:00:00",
  "updated_at": "2026-09-16 20:00:00",
  "items": [
    {
      "id": 1,
      "transfer_id": 1,
      "sku_id": 1,
      "sku_code": "SKU001",
      "product_name": "商品A",
      "spec": "红色/大号",
      "count": 10.00
    }
  ]
}
```

列表元素 = 详情去掉 `items`。`status_text` 映射：`10 草稿 / 20 已完成 / 90 已作废`。

**POST `/api/stock-transfers/{id}/finish`**

- 仅 `status = 10` 可执行，否则 `code=1001`
- 出库仓扣减 + 入库仓增加，走 `change_inventory`，`order_type` 分别用 `TRANSFER_OUT` / `TRANSFER_IN`
- 单号作为流水的业务单号
- 成功后 `status = 20`、`finished_at` 写入，返回详情

**POST `/api/stock-transfers/{id}/cancel`**

- 仅 `status = 10` 可作废，否则 `code=1001`
- 不动库存（草稿从未动过库存）

### 16.4 库存流水新增类型

| `order_type` | 中文 | 触发场景 |
|---|---|---|
| `TRANSFER_OUT` | 移库出 | 执行移库时出库仓扣减 |
| `TRANSFER_IN` | 移库入 | 执行移库时入库仓增加 |

---

## 十七、盘点单（盘库）

### 17.1 设计说明

- 抄 yudao WMS 的盘库单：建单 → 录入实盘 → 完成盘点按差异调整库存
- 建单时选定仓库 + SKU 清单，**自动快照账面数量**（`inventory.quantity`）
- 「完成盘点」时按 `差异 = 实盘 - 账面` 走 `change_inventory`（`order_type = STOCK_TAKE`）
- 状态：`10 盘点中 / 20 已完成 / 90 已作废`；仅「盘点中」可录入实盘、完成、作废
- 与既有 `POST /api/inventory/adjust` 的关系：`adjust` 是**单点即时调整**（无单据），
  盘点单是**成批、有单据留痕**的流程。两者都走 `change_inventory`，互不替代。
- **账面快照与完成时实际库存可能不一致**（盘点期间发生了出入库）。本阶段约定：
  以「快照账面」为基准计算差异并落流水，不做二次校验；`diff_count` 记录该次盘点的差异合计。

### 17.2 新增表

**`stock_take` 盘点单主表**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键，自增 |
| no | varchar(32) | 单号 `ST` + yyyyMMdd + 4 位当日流水，**唯一** |
| warehouse_id | bigint | 盘点仓库 `warehouse.id` |
| status | tinyint | 10 盘点中 / 20 已完成 / 90 已作废 |
| total_count | decimal(14,2) | 账面数量合计（建单时快照） |
| diff_count | decimal(14,2) | 差异合计（实盘 - 账面），完成时写入，未完成时 0 |
| remark | varchar(500) | 备注，可空 |
| created_by | bigint | 制单人 `sys_user.id` |
| finished_at | datetime | 完成盘点时间，可空 |
| created_at / updated_at | datetime | 统一时间戳 |

索引：`no` 唯一索引；`warehouse_id`、`status` 普通索引。

**`stock_take_item` 盘点单明细**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint | 主键，自增 |
| stock_take_id | bigint | 关联 `stock_take.id` |
| sku_id | bigint | 关联 `product_sku.id` |
| book_quantity | decimal(14,2) | 账面数量（建单快照） |
| actual_quantity | decimal(14,2) | 实盘数量，建单时 = 账面数量 |
| diff_quantity | decimal(14,2) | 差异 = 实盘 - 账面，完成时写入 |
| remark | varchar(255) | 行备注（如「破损 2 件」），可空 |
| created_at / updated_at | datetime | 统一时间戳 |

索引：`stock_take_id` 普通索引。

### 17.3 新增接口

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | `/api/stock-takes` | warehouse, admin | 新建盘点单 |
| GET | `/api/stock-takes` | warehouse, admin | 列表，支持 `?page=&page_size=&keyword=&status=&warehouse_id=` |
| GET | `/api/stock-takes/{id}` | warehouse, admin | 详情（含明细） |
| PUT | `/api/stock-takes/{id}/items` | warehouse, admin | 录入实盘数量 |
| POST | `/api/stock-takes/{id}/finish` | warehouse, admin | 完成盘点（按差异调库存） |
| POST | `/api/stock-takes/{id}/cancel` | warehouse, admin | 作废 |

`keyword` 匹配：单号 / 备注。

**POST `/api/stock-takes` 请求**

```json
{
  "warehouse_id": 1,
  "remark": "月末盘点",
  "sku_ids": [1, 2, 3]
}
```

- `sku_ids` 不传或为空数组 → 取该仓库**所有已有库存记录的 SKU**（`inventory` 表里 `warehouse_id` 匹配的行）
- 该仓库没有任何库存记录且未指定 `sku_ids` → `code=1001`「该仓库没有库存记录，无法建盘点单」
- 指定了不存在的 SKU → `code=1001`
- `book_quantity` 取建单时的 `inventory.quantity`（无库存行则记 0）
- `total_count` = 所有行账面数量合计

**PUT `/api/stock-takes/{id}/items` 请求**

```json
{
  "items": [
    { "item_id": 1, "actual_quantity": 98, "remark": "破损 2 件" },
    { "item_id": 2, "actual_quantity": 5 }
  ]
}
```

- 仅 `status = 10` 可录入，否则 `code=1001`
- `actual_quantity >= 0`（Pydantic `ge=0`，违规 `code=400`）
- `item_id` 必须属于该盘点单，否则 `code=1001`「盘点明细不存在」
- 同一 `item_id` 不允许重复行（Pydantic 层，`code=400`）
- 允许部分录入（只传部分行），未传的行保持原值

**POST `/api/stock-takes/{id}/finish`**

- 仅 `status = 10` 可完成，否则 `code=1001`
- 逐行算 `diff = actual_quantity - book_quantity`；`diff != 0` 的行才走 `change_inventory`（`order_type = STOCK_TAKE`，单号作业务单号）
- 差异为负时校验可用量，不足 → `code=1001`（理论上不该发生，作兜底）
- 写回每行 `diff_quantity`；主表 `diff_count` = 差异合计、`status = 20`、`finished_at`
- 全部差异为 0 时也允许完成（不写流水），`diff_count = 0`

**POST `/api/stock-takes/{id}/cancel`**

- 仅 `status = 10` 可作废，否则 `code=1001`
- 不动库存

**响应结构（详情）**

```json
{
  "id": 1,
  "no": "ST202609160001",
  "warehouse_id": 1,
  "warehouse_name": "主仓",
  "status": 10,
  "status_text": "盘点中",
  "total_count": 300.00,
  "diff_count": 0.00,
  "remark": "月末盘点",
  "created_by": 3,
  "created_by_name": "仓管老李",
  "finished_at": null,
  "created_at": "2026-09-16 20:00:00",
  "updated_at": "2026-09-16 20:00:00",
  "items": [
    {
      "id": 1,
      "stock_take_id": 1,
      "sku_id": 1,
      "sku_code": "SKU001",
      "product_name": "商品A",
      "spec": "红色/大号",
      "book_quantity": 100.00,
      "actual_quantity": 100.00,
      "diff_quantity": 0.00,
      "remark": null
    }
  ]
}
```

列表元素 = 详情去掉 `items`。`status_text` 映射：`10 盘点中 / 20 已完成 / 90 已作废`。

### 17.4 库存流水新增类型

| `order_type` | 中文 | 触发场景 |
|---|---|---|
| `STOCK_TAKE` | 盘点调整 | 完成盘点时按差异调整 |

---

## 十八、库存预警（安全库存）

### 18.1 设计说明

抄 `vvsgxmn/WarehouseManage` 的库存预警：在 SKU 上维护安全库存下限，
**可用量低于安全库存**即进入预警列表。

`available_quantity = quantity - reserved_quantity`（沿用第十二节定义）。

### 18.2 表变更：`product_sku` 新增列

| 字段 | 类型 | 说明 |
|---|---|---|
| min_stock | decimal(14,2) | 安全库存下限，`NOT NULL DEFAULT 0.00`；**0 表示不预警** |

`product_sku` 的请求 / 响应 schema（`SkuCreate` / `SkuUpdate` / SKU 序列化）都要带上 `min_stock`：
- `SkuCreate.min_stock`：`Decimal`，`ge=0`，默认 `0.00`
- `SkuUpdate.min_stock`：`Decimal | None`，`ge=0`
- SKU 列表 / 详情 / 下拉选项的响应里都返回 `min_stock`（float）

### 18.3 新增接口

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/api/inventory/alerts` | operator, warehouse, admin | 库存预警列表 |

- 支持 `?page=&page_size=&warehouse_id=`
- 筛选条件：`min_stock > 0 AND (quantity - reserved_quantity) < min_stock`
- 排序：短缺量从大到小
- 响应元素 = 库存列表元素（`inventory_out`）额外加：

| 字段 | 说明 |
|---|---|
| `min_stock` | SKU 的安全库存下限 |
| `shortage` | 短缺量 = `min_stock - available_quantity`，恒 > 0 |

> 注意路由注册顺序：`/api/inventory/alerts` 必须与 `/api/inventory/history` 一样是**静态路径**，
> 现有 `/api/inventory` 下没有 `/{id}` 通配路由，因此不冲突。

### 18.4 前端

- 「库存管理」菜单下新增「库存预警」页：表格 + 仓库筛选 + 分页，短缺量高亮
- SKU 管理的新增 / 编辑表单加「安全库存」输入框（数字，≥ 0，默认 0）
- SKU 列表加「安全库存」列
- 首页看板加一张「库存预警」统计卡（预警条数），点击跳转预警页

---

## 十九、五阶段角色权限补充

| 接口域 | admin | operator | warehouse | approver |
|---|---|---|---|---|
| 移库单（建单 / 列表 / 详情 / 执行 / 作废） | ✅ | ❌ | ✅ | ❌ |
| 盘点单（建单 / 列表 / 详情 / 录入 / 完成 / 作废） | ✅ | ❌ | ✅ | ❌ |
| 库存预警查询 | ✅ | ✅ | ✅ | ❌ |
| SKU 安全库存维护 | ✅ | ❌ | ❌ | ❌ |

---

## 二十、五阶段数据流小结

```
移库：建单(草稿,10) ──执行移库──▶ 出库仓 -N / 入库仓 +N ──▶ 已完成(20)
                                  （TRANSFER_OUT + TRANSFER_IN 两条流水）
      草稿 ──作废──▶ 已作废(90)（不动库存）

盘点：建单(盘点中,10，快照账面) ──录入实盘──▶ ──完成盘点──▶ 按差异调库存 ──▶ 已完成(20)
                                                        （STOCK_TAKE 流水）
      盘点中 ──作废──▶ 已作废(90)（不动库存）

预警：product_sku.min_stock > 0 且 可用量 < min_stock ──▶ /api/inventory/alerts
```
